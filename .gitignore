# The application lives in app/ and has its own .gitignore.
# This file covers only what can appear at the repository root.

.DS_Store
*.log

# Never commit real credentials. The app's own .gitignore repeats this for
# app/.env*, and re-includes .env*.example so the template stays in the repo.
.env
.env.*
!.env*.example
